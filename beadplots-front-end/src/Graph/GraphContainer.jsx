import React from 'react';

const MOCK_DATA = {
  abstract : ['a'],
  introduction : ['b', 'c'],
  methods : ['d', 'e', 'f'],
  findings : ['g', 'h', 'i', 'j'],
  conclusion : ['k', 'l', 'm', 'n', 'o']
}

const GraphContainer = (props) => {
  return props.children
}